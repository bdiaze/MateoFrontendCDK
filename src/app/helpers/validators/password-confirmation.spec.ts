import { PasswordConfirmation } from './password-confirmation';

describe('PasswordConfirmation', () => {
  it('should create an instance', () => {
    expect(new PasswordConfirmation()).toBeTruthy();
  });
});
